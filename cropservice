apiVersion: v1
kind: Service
metadata:
    name: crop-management
    namespace: kisanvedika
    annotations:
#      traefik.backend.circuitbreaker: "NetworkErrorRatio() > 0.5"
spec:
  type: LoadBalancer
  ports:
  - name: http
    targetPort: 80
    port: 80
  selector:
    app: crop-management
