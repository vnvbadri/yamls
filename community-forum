apiVersion: v1
kind: Service
metadata:
    name: community-forum
    namespace: kisanvedika
    annotations:
#      traefik.backend.circuitbreaker: "NetworkErrorRatio() > 0.5"
spec:
  type: LoadBalancer
  ports:
  - name: http
    targetPort: 8081
    port: 80
  selector:
    app: community-forum
